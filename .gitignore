# Dependencias de Node
node_modules/

# Base de datos local de SQLite (para que cada quien tenga la suya o no se pisen datos)
reservas.sqlite
reservas.sqlite-*

# Variables de entorno por si acaso
.env

# Logs de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema operativo
.DS_Store
Thumbs.db